# API base path for axios (Vite dev proxy uses /api by default)
VITE_API_URL=/api

# Backend API origin used for OAuth redirects (must match OAuth callback URLs in provider consoles)
VITE_API_BASE_URL=http://localhost:5000
